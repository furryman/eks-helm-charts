cert-manager:
  crds:
    enabled: true
  prometheus:
    enabled: false
  # ExperimentalGatewayAPISupport feature gate removed: it is BETA with
  # default=true in v1.20.2 and requires no explicit opt-in.

# Let's Encrypt configuration
letsencrypt:
  email: admin@fuhriman.org
  server: https://acme-v02.api.letsencrypt.org/directory
