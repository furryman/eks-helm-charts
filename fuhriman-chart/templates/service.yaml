apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuhriman-chart.fullname" . }}
  labels:
    {{- include "fuhriman-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuhriman-chart.selectorLabels" . | nindent 4 }}
